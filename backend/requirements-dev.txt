-r requirements.txt

pytest==8.3.4
pytest-cov==6.0.0
httpx==0.28.1
ruff==0.8.6
mypy==1.14.1
# Dependency vulnerability audit — run locally when touching requirements.txt;
# also gated in CI (.github/workflows/security.yml).
pip-audit==2.10.1
