# The public npm registry, explicitly.
#
# A corporate proxy inherited from a developer's environment or a CI image
# would make a fresh Codespace fail to install with errors that look like
# network flakiness. Pinning it here means every checkout resolves the same way.
registry=https://registry.npmjs.org/
fund=false
audit=false
